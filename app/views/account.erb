<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if @current_user.is_logged_in?(session) %>
    <label> <h1> Welcome <%= @current_user(session).username%></h1> </label>
    <label> <h3> Your Balance: <%= @current_user(session).balance %> </h3> </label?
      <h1>You Must <a href="/">Log In</a> to View Your Balance</h1>
      <% end %>

     <h3> <a href="/logout"> Log Out </a> </h3>
  </body>
</html>